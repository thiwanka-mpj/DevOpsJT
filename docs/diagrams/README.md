Store exported diagram source files here (e.g. `.drawio`, `.excalidraw`) if you
prefer designing diagrams visually instead of writing Mermaid by hand.
Prefer Mermaid (fenced ```mermaid blocks directly in your .md files) where
possible — it stays version-controlled as text and renders natively.
